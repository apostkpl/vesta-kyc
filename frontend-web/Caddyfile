:80 {
    root * /usr/share/caddy
    encode gzip zstd

    # Support React SPA routing (try exact file, else fall back to index.html)
    try_files {path} /index.html

    # Serve static files
    file_server
}